define(/** @exports core */[
    './adapt',
    './array',
    './base',
    './command',
    './date',
    './facade',
    './factory',
    './format',
    './iter',
    './mediator',
    './notifications',
    './proxy',
    './scheduler',
    './string',
    './uri',
    './view'
], function(
    adapt,
    array,
    base,
    command,
    date,
    facade,
    factory,
    format,
    iter,
    mediator,
    notifications,
    proxy,
    scheduler,
    string,
    uri,
    view) {
    
    return {
        adapt: adapt,
        array: array,
        base: base,
        command: command,
        date: date,
        facade: facade,
        factory: factory,
        format: format,
        iter: iter,
        mediator: mediator,
        notifications: notifications,
        proxy: proxy,
        scheduler: scheduler,
        string: string,
        uri: uri,
        view: view
    };
});
